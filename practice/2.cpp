#include <iostream>
using namespace std;
// just a starting code

int main() {
    int in1, in2;
    cin >> in1 >> in2;
    //results the add,sub,multi,div
    cout << in1 + in2 << " " << in1 - in2 << " " << in1 * in2 << " " << in1 / in2;
}
