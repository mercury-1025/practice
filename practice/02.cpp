#include <iostream>
using namespace std;
// just a starting code
// get add,sub,multi,div of 2 input (+,-,*,/)
int main() {
    int in1, in2;
    cin >> in1 >> in2;
    //results the add,sub,multi,div
    cout << in1 + in2 << " " << in1 - in2 << " " << in1 * in2 << " " << in1 / in2;
}
