#include <iostream>
#include <cmath>
//get third letter of string and its ascii code
using namespace std;
int main() {
	string input;
	cin >> input;
	char in = input[2];
	int ascii = (in);
	cout << in << " " << ascii;
}
