#include <iostream>
using namespace std;
// just a starting code
//get area of triangle and rect from 2 input number
int main() {
    int in1, in2;
    cin >> in1 >> in2;
    // gives area of triangle and rectangle with input number side and hight
    cout << in1 * in2 / 2 << " " << in1 * in2;
}
